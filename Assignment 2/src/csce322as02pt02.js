module.exports = {
    withoutL: withoutL
}

function withoutL(game){
    /*
      Returns a function that, given the position of a piece and a roll will determine where the piece will move
      The presence of ladders is ignored.
     */
    
    function whatever(position,roll){
    }

    return whatever;
}
